naftiko: 1.0.0-alpha2
info:
  label: Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService) — Scripts
  description: 'Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService) — Scripts. 1 operations.
    Lead operation: Microsoft Endpoint Configuration Management List scripts. Self-contained Naftiko capability covering one
    Microsoft Endpoint Configuration Management business surface.'
  tags:
  - Microsoft Endpoint Configuration Management
  - Scripts
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_ENDPOINT_CONFIGURATION_MANAGEMENT_API_KEY: MICROSOFT_ENDPOINT_CONFIGURATION_MANAGEMENT_API_KEY
capability:
  consumes:
  - type: http
    namespace: configmgr-rest-scripts
    baseUri: https://{siteserver}/AdminService
    description: Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService) — Scripts business
      capability. Self-contained, no shared references.
    resources:
    - name: v1.0-Scripts
      path: /v1.0/Scripts
      operations:
      - name: listscripts
        method: GET
        description: Microsoft Endpoint Configuration Management List scripts
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_ENDPOINT_CONFIGURATION_MANAGEMENT_API_KEY}}'
  exposes:
  - type: rest
    namespace: configmgr-rest-scripts-rest
    port: 8080
    description: REST adapter for Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService)
      — Scripts. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v1-0/scripts
      name: v1-0-scripts
      description: REST surface for v1.0-Scripts.
      operations:
      - method: GET
        name: listscripts
        description: Microsoft Endpoint Configuration Management List scripts
        call: configmgr-rest-scripts.listscripts
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: configmgr-rest-scripts-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService)
      — Scripts. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-endpoint-configuration-management-list
      description: Microsoft Endpoint Configuration Management List scripts
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: configmgr-rest-scripts.listscripts
      outputParameters:
      - type: object
        mapping: $.
