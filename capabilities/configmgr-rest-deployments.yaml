naftiko: 1.0.0-alpha2
info:
  label: Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService) — Deployments
  description: 'Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService) — Deployments. 1
    operations. Lead operation: Microsoft Endpoint Configuration Management List application deployments. Self-contained Naftiko
    capability covering one Microsoft Endpoint Configuration Management business surface.'
  tags:
  - Microsoft Endpoint Configuration Management
  - Deployments
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_ENDPOINT_CONFIGURATION_MANAGEMENT_API_KEY: MICROSOFT_ENDPOINT_CONFIGURATION_MANAGEMENT_API_KEY
capability:
  consumes:
  - type: http
    namespace: configmgr-rest-deployments
    baseUri: https://{siteserver}/AdminService
    description: Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService) — Deployments business
      capability. Self-contained, no shared references.
    resources:
    - name: wmi-SMS_ApplicationDeployment
      path: /wmi/SMS_ApplicationDeployment
      operations:
      - name: listdeployments
        method: GET
        description: Microsoft Endpoint Configuration Management List application deployments
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_ENDPOINT_CONFIGURATION_MANAGEMENT_API_KEY}}'
  exposes:
  - type: rest
    namespace: configmgr-rest-deployments-rest
    port: 8080
    description: REST adapter for Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService)
      — Deployments. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/wmi/sms-applicationdeployment
      name: wmi-sms-applicationdeployment
      description: REST surface for wmi-SMS_ApplicationDeployment.
      operations:
      - method: GET
        name: listdeployments
        description: Microsoft Endpoint Configuration Management List application deployments
        call: configmgr-rest-deployments.listdeployments
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: configmgr-rest-deployments-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Endpoint Configuration Management Configuration Manager REST API (AdminService)
      — Deployments. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-endpoint-configuration-management-list
      description: Microsoft Endpoint Configuration Management List application deployments
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: configmgr-rest-deployments.listdeployments
      outputParameters:
      - type: object
        mapping: $.
